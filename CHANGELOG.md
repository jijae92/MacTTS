# Changelog

## Unreleased
- Documented CLI/GUI entry points with cross-references to README and added `ARCHITECTURE.md`.
- Added `mac_e2e_test.sh` + PyInstaller spec entry shim (`gui_entry.py`) for the macOS workflow.
- Added `check_pyside.py`, GUI ffmpeg warnings, README macOS dependency guidance, and audio mock tests to guarantee macOS dependency behavior.
